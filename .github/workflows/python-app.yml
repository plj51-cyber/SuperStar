#此工作流将安装Python依赖项、运行测试并使用单个Python版本进行lint
#有关详细信息，请参阅： https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

步骤

run-on:ubuntu-latest
python版
分支机构：["主要"]
pull_request：
分支机构：["主要"]

权限：
内容：已读

作业：
建立：

run-on:ubuntu-latest

步骤:
-uses: 操作/结帐@v4
-姓名: 设置Python3.10
uses: actions/setup-python@v3
和……一起:
#exit-zero将所有错误视为警告。GitHub编辑器的宽度为127个字符python版本：“3.10”
-姓名: 安装相依性
跑: |
Python-m pip安装--升级pip
PIP安装薄片8pytest
如果[-f要求.文本]；然后pip安装-r要求.txt；fi
-姓名: 带鳞片的棉绒8
运行：|
        #如果存在Python语法错误或未定义的名称，则停止生成
        fLake8. --计数--选择=E9、F63、F7、F82--显示源--统计
        #exit-zero将所有错误视为警告。GitHub编辑器的宽度为127个字符
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    - name: Test with pytest
      run: |
        pytest
